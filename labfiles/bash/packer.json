{
    "variables": {
        "aws_access_key": "AKIAQ4IGF7IIGQGHK4UF",
        "aws_secret_key": "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",
        "aws_subnet_id": "subnet-06e649cbfc7405f8c"
    },
    "builders": [
        {
            "type": "amazon-ebs",
            "access_key": "{{user `aws_access_key`}}",
            "secret_key": "{{user `aws_secret_key`}}",
            "subnet_id": "{{user `aws_subnet_id`}}",
            "region": "us-east-1",
            "instance_type": "t2.micro",
            "ami_name": "bookapp-{{timestamp}}",
            "source_ami_filter": {
                "filters": {
                    "virtualization-type": "hvm",
                    "name": "*ubuntu/images/*ubuntu-focal-20.04-amd64-server-*",
                    "root-device-type": "ebs"
                },
                "owners": ["099720109477"],
                "most_recent": true
            },
            "security_group_filter": {
                "filters": {
                    "tag:Class": "packer"
                }
            },
            "ssh_username": "ubuntu"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "inline": [
                                "sudo apt-get update -y && sudo apt-get upgrade -y",
                                "sudo mkdir /opt/provisioning",
                                "sudo chown ubuntu:ubuntu /opt/provisioning"
                                ]
        },
        {
            "type": "shell",
            "scripts": [
                "scripts/init.sh",
                "scripts/webdeploy.sh"
            ],
            "environment_vars": ["HOSTNAME=bookapp"],
            "remote_folder": "/opt/provisioning",
            "skip_clean": true
        }
    ]
}
